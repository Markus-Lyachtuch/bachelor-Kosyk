.line
    width 100%
    height: 7px
    position relative
    border-radius: 2px
    background: var(--secondary-light)

    &-current-progress
        width 0
        height: 100%
        border-radius: 2px
        transition: all 1s ease-in-out
        background: var(--primary-light-2)